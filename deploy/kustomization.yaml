apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: clusterscape

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# CI rewrites newTag on every push to main; Argo CD rolls it out.
images:
  - name: harbor.lan/apps/clusterscape
    newTag: ba8649e7fcdf
